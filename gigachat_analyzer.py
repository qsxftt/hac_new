# -*- coding: utf-8 -*-
# gigachat_analyzer.py
from gigachat import GigaChat
from gigachat.models import Chat, Messages, MessagesRole
from gigachat.exceptions import GigaChatException
import logging
import json
import re
from config import config

logger = logging.getLogger(__name__)

class GigaChatAnalyzer:
    def __init__(self):
        self.api_key = config.GIGACHAT_API_KEY
        self.temperature = config.GIGACHAT_TEMPERATURE
        self.max_tokens = config.GIGACHAT_MAX_TOKENS
        self.model = config.GIGACHAT_MODEL
        self._client = None
    
    @property
    def client(self):
        if self._client is None:
            try:
                self._client = GigaChat(
                    credentials=self.api_key,
                    verify_ssl_certs=False,
                    timeout=60,
                    model=self.model
                )
            except Exception as e:
                logger.error(f"Ошибка инициализации GigaChat: {e}")
                self._client = None
        return self._client
    
    def analyze_speech(self, transcript_text, analysis_results, scenario=None):
        try:
            logger.info(f"[SCENARIO] Получен scenario: {scenario}")
            prompt, is_custom = self._build_prompt(transcript_text, analysis_results, scenario)
            logger.info(f"📋 Режим: {'CUSTOM' if is_custom else 'STANDARD'}")

            if not self.client:
                logger.warning("GigaChat клиент не инициализирован, используем локальный фидбэк")
                return self._get_fallback_feedback(analysis_results)

            temp = self.temperature
            if is_custom:
                temp = min(self.temperature, 0.2)

            system_msg = Messages(
                role=MessagesRole.SYSTEM,
                content=(
                    "Ты — продвинутый ассистент, эксперт по анализу публичных выступлений. "
                    "Строго следуй инструкциям в пользовательском сообщении. "
                    "Особенно важно сохранять точный формат, когда пользователь предоставляет свои критерии."
                )
            )

            user_msg = Messages(
                role=MessagesRole.USER,
                content=prompt
            )

            chat_request = Chat(
                messages=[system_msg, user_msg],
                temperature=temp,
                max_tokens=self.max_tokens,
                model=self.model
            )

            response = self.client.chat(chat_request)
            feedback = response.choices[0].message.content.strip()

            logger.info("[FEEDBACK] Фидбэк от GigaChat получен")
            return feedback
            
        except TypeError as e:
            logger.warning(f"Библиотека не поддерживает параметры: {e}. Используем упрощенный запрос.")
            return self._simple_chat_request(prompt, analysis_results)
        except GigaChatException as e:
            logger.error(f"Ошибка GigaChat API: {e}")
            return self._get_fallback_feedback(analysis_results)
        except Exception as e:
            logger.error(f"Неизвестная ошибка: {e}")
            return self._get_fallback_feedback(analysis_results)
    
    def _simple_chat_request(self, prompt, analysis_results):
        try:
            with self.client as giga:
                response = giga.chat(prompt)
                feedback = response.choices[0].message.content.strip()
                logger.info("[FEEDBACK] Фидбэк от GigaChat получен (упрощенный метод)")
                return feedback
        except Exception as e:
            logger.error(f"Ошибка упрощенного запроса: {e}")
            return self._get_fallback_feedback(analysis_results)
    
    def _build_prompt(self, transcript_text, analysis_results, scenario=None):
        max_transcript_length = 3000
        if len(transcript_text) > max_transcript_length:
            transcript_preview = transcript_text[:max_transcript_length] + "... [текст сокращен для анализа]"
        else:
            transcript_preview = transcript_text

        # Определяем тип сценария
        scenario_type = None
        scenario_text = None
        
        if scenario:
            if isinstance(scenario, dict):
                scenario_type = (scenario.get('type') or '').lower()
                scenario_text = scenario.get('text')
            else:
                scenario_type = str(scenario).lower()
        
        logger.info(f"📊 Тип сценария: {scenario_type}, наличие текста: {bool(scenario_text)}")
        if scenario_text:
            logger.info(f"📝 Первые 100 символов критериев: {scenario_text[:100]}")

        # Определяем, это custom или нет
        is_custom = scenario_type == 'custom'

        # Формируем промт в зависимости от типа сценария
        if is_custom:
            # Custom-режим: сохраняем точный формат пользователя + добавляем технические метрики
            tech_metrics = self._format_tech_metrics(analysis_results)
            
            # Форматируем критерии с нумерацией для явной оценки
            criteria_lines = []
            if scenario_text:
                for idx, line in enumerate(scenario_text.strip().split('\n'), 1):
                    line = line.strip()
                    if line:
                        criteria_lines.append(f"{idx}. {line}")
            
            formatted_criteria = '\n'.join(criteria_lines) if criteria_lines else scenario_text
            
            prompt = f"""Ты — опытный тренер по публичным выступлениям. Твоя задача — дать полный развернутый фидбэк по КАЖДОМУ из критериев пользователя.

⚠️ KRITICHESKI VAZHNO: Analiziray vypuskanije PO KAZHDOMU KRITERIYU otdelno i davay konkretnuyu ocenku!

═════════════════════════════════════════════════════════════════

📋 КРИТЕРИИ ОЦЕНКИ (от пользователя):

{formatted_criteria}

═════════════════════════════════════════════════════════════════

📊 ТЕХНИЧЕСКИЕ МЕТРИКИ РЕЧИ:
{tech_metrics}

═════════════════════════════════════════════════════════════════

📝 ТЕКСТ ВЫСТУПЛЕНИЯ ДЛЯ АНАЛИЗА:
{transcript_preview}

═════════════════════════════════════════════════════════════════

✅ ИНСТРУКЦИИ ДЛЯ ОТВЕТА:

1️⃣ ОБЯЗАТЕЛЬНО оцени КАЖДЫЙ критерий из списка выше:
   - Используй формат: "✓/✗ [Номер критерия]: [Название] - [Оценка и комментарий]"
   - Будь конкретным, ссылайся на конкретные места в тексте
   - Дай оценку от 1 до 5 звёзд (⭐) для каждого критерия

2️⃣ Добавь анализ ТЕХНИЧЕСКИХ аспектов:
   - Соответствие метрик оптимальным значениям
   - Влияние техники речи на общее впечатление

3️⃣ Напиши КОНКРЕТНЫЕ рекомендации для улучшения по каждому критерию

4️⃣ В конце дай ИТОГОВУЮ оценку выступления (как сумма оценок по критериям)

═════════════════════════════════════════════════════════════════

ФОРМАТ ОТВЕТА:

# 📋 Анализ по критериям пользователя

## Критерий 1: [название]
⭐⭐⭐⭐ (4/5)
[Развёрнутый анализ, ссылки на конкретные места в тексте, рекомендации]

## Критерий 2: [название]
⭐⭐⭐ (3/5)
[Развёрнутый анализ...]

[... остальные критерии ...]

---

# 🎤 Технические аспекты речи
[Анализ темпа, пауз, слов-паразитов, повторений]

---

# 💡 Общие рекомендации
1. [Рекомендация по приоритету]
2. [Рекомендация по приоритету]
...

---

# 🏆 ИТОГОВАЯ ОЦЕНКА: X/5 (средняя по всем критериям)
[Краткое резюме и слова поддержки]

═════════════════════════════════════════════════════════════════

ПОМНИ: Фидбэк должен быть КОНКРЕТНЫМ, ПОЛЕЗНЫМ и ЧЕСТНЫМ! Не пропускай ни один критерий!
"""
        else:
            # Стандартные типы работ
            structure_templates = {
                'academic': {
                    'name': 'Академические работы',
                    'structure': """ОЖИДАЕМАЯ СТРУКТУРА АКАДЕМИЧЕСКОГО ВЫСТУПЛЕНИЯ:
    1. Введение (актуальность, проблема, цель работы)
    2. Литературный обзор или теоретическая база
    3. Методология исследования
    4. Результаты и их анализ
    5. Выводы и перспективы дальнейших исследований"""
                },
                'project': {
                    'name': 'Защита проекта',
                    'structure': """ОЖИДАЕМАЯ СТРУКТУРА ЗАЩИТЫ ПРОЕКТА:
    1. Описание проблемы или возможности
    2. Анализ рынка и конкурентов
    3. Предлагаемое решение
    4. Архитектура и реализация
    5. Бизнес-модель и экономическая эффективность
    6. Дорожная карта (roadmap)"""
                },
                'hackathon': {
                    'name': 'Хакатон/питч',
                    'structure': """ОЖИДАЕМАЯ СТРУКТУРА ПИТЧ-ВЫСТУПЛЕНИЯ:
    1. Хук/привлечение внимания (первые 30 секунд)
    2. Описание проблемы (боли аудитории)
    3. Предлагаемое решение (продукт)
    4. Демонстрация прототипа или концепции
    5. Рассказ о команде и уникальности
    6. Призыв к действию"""
                },
                'public': {
                    'name': 'Публичное выступление',
                    'structure': """ОЖИДАЕМАЯ СТРУКТУРА ПУБЛИЧНОГО ВЫСТУПЛЕНИЯ:
    1. Приветствие и анонс темы
    2. Основная часть с ключевыми тезисами
    3. Примеры, истории, иллюстрации
    4. Заключение и основные выводы
    5. Ответы на вопросы (Q&A)"""
                }
            }
            
            # Получаем структуру для выбранного типа
            template = structure_templates.get(scenario_type, structure_templates['academic'])
            
            # Форматируем результаты анализа для стандартных типов
            analysis_summary = f"""
    ТЕХНИЧЕСКИЕ МЕТРИКИ:
    🕒 Средний темп речи: {analysis_results.get('avg_tempo', 0):.1f} слов/сек
    ⏱ Длительность выступления: {analysis_results.get('total_duration', 0):.1f} сек
    🗣 Всего слов: {analysis_results.get('total_words', 0)}
    ⏸ Длинные паузы (>1 сек): {analysis_results.get('pauses_count', 0)}
    💬 Слова-паразиты: {analysis_results.get('filler_words_count', 0)}
    🔁 Повторяющиеся слова: {analysis_results.get('repetitions_count', 0)}
    """

            structure_analysis = f"""
    ТИП ВЫСТУПЛЕНИЯ: {template['name']}
    
    {template['structure']}
    
    ПРОАНАЛИЗИРУЙ, СООТВЕТСТВУЕТ ЛИ ВЫСТУПЛЕНИЕ ЭТОЙ СТРУКТУРЕ:
    1. Все ли разделы присутствуют?
    2. Правильно ли раскрыты разделы?
    3. Логичны ли переходы между разделами?
    4. Насколько четко сформулированы ключевые моменты?
    """

            prompt = f"""
    Ты — профессиональный тренер по публичным выступлениям с 20-летним опытом.
    Проанализируй это выступление и дай полезный, конструктивный фидбэк.

    КОНТЕКСТ:
    Это тренировочное выступление. Цель — помочь спикеру улучшить навыки публичных выступлений.

    ТЕКСТ ВЫСТУПЛЕНИЯ:
    {transcript_preview}

    {analysis_summary}

    АНАЛИЗ СТРУКТУРЫ ВЫСТУПЛЕНИЯ:
    {structure_analysis}

    ИНСТРУКЦИИ ДЛЯ ОТВЕТА:
    1. Сначала дай оценку структуры выступления
    2. Затем оцени технические аспекты (темп, паузы и т.д.)
    3. Используй Markdown-разметку для структурирования ответа
    4. Используй эмодзи для наглядности
    5. Будь конкретным и давай практические рекомендации

    ФОРМАТ ОТВЕТА:

    # 🎯 Анализ структуры выступления

    ## 📊 Соответствие формату: X/10
    [Твоя оценка соответствия структуры]

    ## [Результат] Полнота разделов:
    - [Раздел 1]: [оценка]
    - [Раздел 2]: [оценка]
    - [Раздел 3]: [оценка]

    ## 🎤 Технические аспекты речи:
    [Анализ темпа, пауз, слов-паразитов]

    ## 💡 Конкретные рекомендации:
    1. [Рекомендация 1]
    2. [Рекомендация 2]
    3. [Рекомендация 3]

    ## 🏆 Общая оценка: X/10
    [Краткое резюме и слова поддержки]
    """
        
        return prompt, is_custom
    
    def _format_tech_metrics(self, analysis_results):
        """Форматирует технические метрики для вставки в промт"""
        tempo = analysis_results.get('avg_tempo', 0)
        pauses = analysis_results.get('pauses_count', 0)
        fillers = analysis_results.get('filler_words_count', 0)
        repetitions = analysis_results.get('repetitions_count', 0)
        duration = analysis_results.get('total_duration', 0)
        words = analysis_results.get('total_words', 0)
        
        # Форматируем оценку темпа
        tempo_eval = "слишком быстро" if tempo > 5 else "слишком медленно" if tempo < 2 else "оптимальный"
        
        # Форматируем оценку пауз
        pauses_eval = "очень много" if pauses > 10 else "много" if pauses > 5 else "нормально" if pauses > 0 else "отлично"
        
        # Форматируем оценку слов-паразитов
        fillers_eval = "очень много" if fillers > 15 else "много" if fillers > 7 else "нормально" if fillers > 0 else "отлично"
        
        # Форматируем оценку повторов
        reps_eval = "много повторов" if repetitions > 5 else "немного" if repetitions > 0 else "отлично"
        
        # ========== НОВОЕ: Аудио метрики ==========
        audio_section = ""
        
        # Проверяем наличие аудио данных (они могут быть внутри analysis_results или отдельно)
        if 'audio_features' in analysis_results or 'energy_score' in analysis_results:
            audio = analysis_results.get('audio_features', analysis_results)
            
            energy = audio.get('energy_score', 0)
            volume = audio.get('avg_volume', 0)
            pitch_var = audio.get('pitch_variance', 0)
            
            if energy > 0:  # Если есть данные
                energy_eval = "высокая (эмоционально)" if energy > 70 else "средняя" if energy > 40 else "низкая (монотонно)"
                volume_eval = "громко" if volume > 70 else "нормально" if volume > 40 else "тихо"
                pitch_eval = "разнообразная" if pitch_var > 500 else "умеренная" if pitch_var > 200 else "монотонная"
                
                audio_section = f"""
🎤 АУДИО ХАРАКТЕРИСТИКИ:
• Энергия речи: {energy:.1f}/100 ({energy_eval})
• Средняя громкость: {volume:.1f}% ({volume_eval})
• Вариация интонации: {pitch_eval}
"""
    
        return f"""
🎤 ТЕМП РЕЧИ:
• Средняя скорость: {tempo:.1f} слов в секунду ({tempo_eval} - оптимально 3-5 слов/сек)

⏸ ПАУЗЫ:
• Длинные паузы (>1 секунды): {pauses} ({pauses_eval})

🗣 СЛОВА-ПАРАЗИТЫ:
• Обнаружено слов-паразитов: {fillers} ({fillers_eval})

🔁 ПОВТОРЫ:
• Слов, повторяющихся более 3 раз: {repetitions} ({reps_eval})

📊 ОБЩАЯ СТАТИСТИКА:
• Длительность выступления: {duration:.1f} секунд
• Всего произнесено слов: {words}
{audio_section}
"""

    
    def _get_fallback_feedback(self, analysis_results):
        """Возвращает фидбэк, если GigaChat недоступен"""
        tempo = analysis_results.get('avg_tempo', 0)
        pauses = analysis_results.get('pauses_count', 0)
        fillers = analysis_results.get('filler_words_count', 0)
        repetitions = analysis_results.get('repetitions_count', 0)
        
        # Рассчитываем оценку
        score = 10.0
        if tempo > 5: score -= 1.5
        if pauses > 5: score -= 1.0
        if fillers > 10: score -= 2.0
        if repetitions > 3: score -= 1.5
        if score < 3: score = 3.0
        
        feedback = f"""
# ОЦЕНКА ВЫСТУПЛЕНИЯ (локальный анализ)

## 📊 Технические метрики
- **Темп речи:** {tempo:.1f} слов/сек ({"слишком быстро" if tempo > 5 else "в норме" if tempo > 2 else "слишком медленно"})
- **Паузы:** {pauses} длинных пауз (>1 сек) - {"много" if pauses > 5 else "нормально" if pauses > 0 else "отлично"}
- **Слова-паразиты:** {fillers} слов - {"очень много" if fillers > 15 else "много" if fillers > 7 else "нормально" if fillers > 0 else "отлично"}
- **Повторы:** {repetitions} слов повторяются >3 раз

## 💡 Рекомендации:
1. Поддерживайте темп 3-4 слова в секунду
2. Делайте осознанные паузы для акцента, но не длиннее 2 секунд
3. Заменяйте слова-паразиты на осмысленные паузы
4. Используйте синонимы для часто повторяющихся слов
5. Практикуйтесь с таймером и записывайте себя на видео

## 🏆 Общая оценка: {score:.1f}/10
Продолжайте работать над своими навыками! Регулярная практика - ключ к успеху.
"""
        
        return feedback

# Создаем экземпляр анализатора
analyzer = GigaChatAnalyzer()